# Dependencies
node_modules/

# Installed separately by zzc-skills; these links are machine-local
skills/

# Downloaded/generated executables (file-search fallback binaries)
bin/

# Pi runtime and private state
sessions/
run-history.jsonl
auth.json
trust.json
models.json
models-store.json
npm/
git/

# settings.json is tracked on purpose: this repository is the portable
# configuration linked at ~/.pi/agent; runtime and private state stay ignored.
!settings.json

# Environment variables and secrets
.env
.env.*
*.pem
*.key

# Logs and temporary files
*.log
*.tmp
*.swp
.DS_Store

# Local editor settings
.vscode/
.idea/
