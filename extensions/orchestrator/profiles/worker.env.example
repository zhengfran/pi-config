# Worker profile: omit all ZZC_ORCHESTRATOR_SOCKET/CLIENT_ID/TOKEN variables.
# The extension remains inactive without ZZC_ORCHESTRATOR_PROFILE=global|project.
# Keep normal concrete-work tools; it has no delegation or daemon credential.
