# Launch direct workers through ./pi-worker.mjs, e.g.:
#   PI_WORKER_BINARY=pi ./pi-worker.mjs --model provider/model --approve
# It removes all ZZC_ORCHESTRATOR_* credentials and passes --no-extensions,
# retaining Pi's built-in concrete-work tools but no delegation extensions.
